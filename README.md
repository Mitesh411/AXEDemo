# AXEDemo

## Accessibility checks

This project uses **pa11y-ci** to run automated accessibility checks.

### Local usage

```bash
npm install
npm run start
npm run a11y
```

### Tooling versions

- Node.js: `>=20`
- pa11y-ci: `^4.0.1`
- http-server: `^14.1.1`
